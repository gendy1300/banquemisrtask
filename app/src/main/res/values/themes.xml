<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BanqueMisrTask" parent="Theme.AppCompat.NoActionBar">


        <item name="android:windowBackground">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <!--        <item name="android:colorBackground">@color/sigma_black</item>-->
        <item name="android:includeFontPadding">false</item>
        <item name="android:textColor">@color/black</item>

        <!--        <item name="android:buttonStyle">@style/sigmaButton</item>-->
        <item name="android:scrollbars">none</item>

    </style>

    <style name="Theme.BanqueMisrTask.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.BanqueMisrTask.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.BanqueMisrTask.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>